name: Main workflow

on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Docker part
        run: |
          docker build -t image .
          docker run -v "$PWD"/CV:/code image